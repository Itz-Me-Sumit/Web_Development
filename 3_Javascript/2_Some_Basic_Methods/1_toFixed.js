/*
    .toFixed()

    we have -> 23.56241, if we want only till 2 decimal
    then use---
        .toFixed(how many numbers we want after decimal)

        Note-> returns string
*/

let a=23.56241;
console.log(a.toFixed(3));