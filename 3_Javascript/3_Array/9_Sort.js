// array.sort();
//      -- it sort elements as charector(string_wise)
//      -- not work as number_sorting

const arr1 = ["rob" ,"alice" , "sumit" ,"bob"  ];

const arr1_sort = arr1.sort();
console.log(arr1_sort);


// sorting number.
const arr=[584,29,22,53,123,14,13,0,82,74,94];
arr.sort((a,b)=>a-b);
console.log(arr);